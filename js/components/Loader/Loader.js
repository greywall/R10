import React from 'react';
import {ActivityIndicator} from 'react-native';
import {colors} from '../../config/styles';

const Loader = () => <ActivityIndicator size="large" color="colors.purple" />;

export default Loader;
