import React from 'react';
import {Text, ScrollView} from 'react-native';

const Schedule = () => {
  return (
    <ScrollView>
      <Text>Schedule</Text>
    </ScrollView>
  );
};

export default Schedule;
