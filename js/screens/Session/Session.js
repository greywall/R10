import React from 'react';
import {Text, ScrollView} from 'react-native';

const Session = () => {
  return (
    <ScrollView>
      <Text>Session</Text>
    </ScrollView>
  );
};

export default Session;
