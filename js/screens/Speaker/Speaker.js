import React from 'react';
import {Text, ScrollView} from 'react-native';

const Speaker = () => {
  return (
    <ScrollView>
      <Text>Speaker</Text>
    </ScrollView>
  );
};

export default Speaker;
